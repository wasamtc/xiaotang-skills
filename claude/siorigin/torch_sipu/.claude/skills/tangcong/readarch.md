---
name: readarch
description: >-
  Read docs/tile_extension.md for SIPU instruction set architecture reference.
  ONLY use when the user explicitly types /readarch in their message. Do NOT
  trigger automatically or for any other reason. This skill complements the
  readarch-sdk skill (which searches SDK headers for intrinsic functions and
  APIs) — both should be invoked together on /readarch.
---

通过阅读docs/tile_extension.md，了解SIPU指令集架构。
同时调用readarch-sdk skill查阅SDK底层函数和API。